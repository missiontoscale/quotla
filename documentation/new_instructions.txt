A. For all chats, whether inline or sidebar, the scroll should only exist when user clicks it, otherwise, it looks ugly

B. when user clicks button on bottom right, it should have a close button x at its top right. 

C. Settings should NOt be a nav link for desktop view

