-- Check current admin status
SELECT email, is_admin FROM profiles LIMIT 5;

-- Update your account to admin (replace with your email)
-- UPDATE profiles SET is_admin = true WHERE email = 'your-email@example.com';
